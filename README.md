# Database-key-value_in_cpp
A Database key-value program in c++ using std::map.
It uses map database for creating databases.
In this there are 5 options which are put, get, list, make database and quit.
